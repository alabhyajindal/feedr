<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title || "Feedr" %></title>
    <script src="https://unpkg.com/htmx.org@1.9.10" integrity="sha384-D1Kt99CQMDuVetoL1lrYwg5t+9QdHe7NLX/SoJYkXDFfX37iInKRy5xLSi8nO7UC" crossorigin="anonymous"></script>
    <script src="https://unpkg.com/htmx.org/dist/ext/json-enc.js"></script>

    <style>
      body {
        font-family: sans-serif;
        color:'#343a40'
      }

      a:active {
        color: inherit;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: start;
        gap: 3em;
      }

      nav {
        display: flex;
        gap: 2em;
      }

      .active-link {
        text-decoration: none;
        color: inherit;
      }

      #feedr-text {
        text-decoration: none;
        color: inherit;
      }
    </style>

  </head>
  <body>
    <header>
      <h1><a id='feedr-text' href='/'>Feedr</a></h1>

      <nav>
        <a href="/" class=<%= 'active-link' if request.path == '/' %>>Home</a>
        <a href='/guide' class=<%= 'active-link' if request.path == '/guide' %>>Guide</a>
        <a href='/feeds' class=<%= 'active-link' if request.path == '/feeds' %>>My feeds</a>
    </nav>
    </header>

    

    <main>
      <%= yield %>
    </main>

        </body>
      </html>
